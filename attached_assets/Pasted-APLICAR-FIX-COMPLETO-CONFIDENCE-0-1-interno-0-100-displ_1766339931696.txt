APLICAR FIX COMPLETO CONFIDENCE (0..1 interno / 0..100 display+ML) + NORMALIZACIÓN AL LEER DB

Contexto: hoy se mezclan escalas 0..1 y 0..100. Esto genera bugs silenciosos (scale-out nunca se activa, dataset IA inconsistente, tests contaminan).

Cambios requeridos (OBLIGATORIOS para cerrar el fix):

1) Crear server/utils/confidence.ts con:
- toConfidencePct(value, fallback=50): number   // devuelve 0..100, acepta 0..1 o 0..100
- toConfidenceUnit(value, fallback=0): number  // devuelve 0..1, acepta 0..1 o 0..100

2) NORMALIZACIÓN AL LEER POSICIONES (crítico):
En server/services/tradingEngine.ts, al reconstruir OpenPosition desde DB:
- Reemplazar parseFloat(pos.signalConfidence) por:
  signalConfidence: pos.signalConfidence ? toConfidenceUnit(pos.signalConfidence) : undefined

Objetivo: cualquier valor histórico ("0.78", "78", "80") acaba interno en 0..1.

3) Fix SMART_GUARD scale-out:
En checkSmartGuardExit (bloque SCALE-OUT):
- Convertir a % ambos lados:
  confPct = toConfidencePct(position.signalConfidence, 0)
  thresholdPct = toConfidencePct(scaleOutThreshold, 80)
- Comparar confPct >= thresholdPct
- En sellReason/logs mostrar confPct (ej. 78%) y no 0.78%.

4) Fix dataset IA:
Donde se construyen AI samples/features:
- confidence debe guardarse SIEMPRE en 0..100:
  confidence: toConfidencePct(signalConfidence, 50)

5) Fix endpoint test /api/test/create-position:
- Cambiar signalConfidence: 80 -> 0.8
  (o usar toConfidenceUnit(80))

Criterios de aceptación (tests rápidos):
A) Posición DB con signalConfidence="80" -> al cargar, position.signalConfidence debe ser 0.8
B) Posición DB con signalConfidence="0.78" -> al cargar, position.signalConfidence debe ser 0.78
C) Logs/Telegram que muestren % deben mostrar 78% (no 0.78%)
D) AI sample confidence debe quedar en rango 0..100 (ej. 78), nunca 0.78
E) Ningún cambio en estrategias: siguen devolviendo confidence 0..1 (sin tocar)

Entregable:
- PR con confidence.ts + cambios en tradingEngine.ts + routes.ts (test endpoint)
